export enum IBookingStatus {
  CONFIRMED = 'confirmed',
  CANCELED = 'canceled',
  COMPLETED = 'completed',
  RESCHEDULE_PENDING = 'reschedule_pending',
}

export enum IPaymentStatus {
  PENDING = 'pending',
  CANCELED = 'canceled',
  COMPLETED = 'completed',
}
